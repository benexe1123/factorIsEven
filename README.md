# factorIsEven
An isEven function which determines if a number is even after computing every positive factor of the number and checking if 2 is a factor. (This is computationally expensive for most numbers versus traditional means).
